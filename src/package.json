{
  "name": "docker-multi-stage-app",
  "version": "1.0.0",
  "description": "Multi-stage Docker application example",
  "main": "app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon --inspect=0.0.0.0:9229 src/app.js",
    "build": "echo 'No build required for Node.js'",
    "test": "jest --coverage",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "keywords": [
    "docker",
    "compose",
    "multi-stage",
    "node.js"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "pg": "^8.10.0",
    "redis": "^4.6.10",
    "pino": "^8.16.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "eslint": "^8.50.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
